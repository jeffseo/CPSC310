<%= form_for(@history_entry) do |f| %>
  <% if @history_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history_entry.errors.count, "error") %> prohibited this history_entry from being saved:</h2>

      <ul>
      <% @history_entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_time %><br>
    <%= f.datetime_select :date_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
